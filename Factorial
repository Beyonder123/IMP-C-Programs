#include <stdio.h>

int main()
{
    int n,i;
    int j = 1 ;
    printf("Enter the number: ");
    scanf("%d", &n);
    
    for(i=1; i<=n; i++){
        j= j*i;
        
    }
    
    printf("Factorial is: %d",j);
    return 0;
}
